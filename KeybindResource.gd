class_name KeybindResource
@export var UP_KEY = InputEventKey.new()
@export var DOWN_KEY = InputEventKey.new()
@export var LEFT_KEY = InputEventKey.new()
@export var RIGHT_KEY = InputEventKey.new()
@export var PAUSE_KEY = InputEventKey.new()
# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
